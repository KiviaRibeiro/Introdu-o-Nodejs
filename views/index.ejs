<!DOCTYPE html>
 <html>
     <head>
         <title>Meu App</title>
         <meta charset="utf-8">
     </head>
     <body>
        <h2>Cliente Cadastrados:</h2>
        <ul>
            <% lista.forEach(function(user){ %>
              <li><%= user.nome %><li> 
            <% }) %> 
        </ul>
     </body>
 </html>